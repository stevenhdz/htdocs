'use strict';
import { StyleSheet } from 'react-native';

export const stylesInt = StyleSheet.create({
    TextTitle: {
        fontFamily: "OpenSans-Semibold-webfont",
        fontSize: 28,
        marginBottom: 4,
        marginTop: 4,
        textAlign: 'center',
    }
})