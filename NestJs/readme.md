```sh
npm i @nestjs/cli -g
```

```sh
nest new nestApp
```

```sh
cd nest-app
npm run start:dev
```

```sh
nest generate module tasks
```

```sh
nest generate controller tasks --no-spec
```

```sh
 nest generate service tasks --no-spec
```