function processPaymentWithCard(price, cardDetails) {
    console.log(`Process payment of $${price} with credits card`)
}

function processPaymentWithPayPal(price, cardDetails) {
    console.log(`Process payment of $${price} with credits card`)
}

function processPaymentWithBitcoin(price, cardDetails) {
    console.log(`Process payment of $${price} with credits card`)
}