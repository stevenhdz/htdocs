chmod +x index.js

npm run dev -- base64 "hola mundo"
npm run dev -- sha256 "hola mundo"

npm install -g .

npm ls -g --depth=0

npm uninstall -g cli

crypto-cli base64 "probando el CLI"
crypto-cli sha256 "probando el CLI"
