const miLista = [1, 2, 3, 4, 5];

// Operaciones con lista
miLista.push(6);    // Agregar al final
miLista.unshift(0);  // Agregar al principio
miLista.pop();       // Eliminar el último elemento
miLista.shift();     // Eliminar el primer elemento

console.log(miLista);